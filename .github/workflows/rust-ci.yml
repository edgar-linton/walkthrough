name: Rust CI

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

env:
  CARGO_TERM_COLOR: always

jobs:
  checks-and-docs:
    name: Format, Clippy & Docs (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-rust
        with:
          components: rustfmt, clippy
      - run: cargo fmt --all -- --check
      - run: cargo clippy --all-targets --all-features -- -D warnings
      # Both feature configurations: the featureless build never compiles the
      # `async` module, so links inside it are only checked with the feature on,
      # and links into it from the sync half only without it. Broken intra-doc
      # links are denied at the crate root, so either build fails on one.
      - run: cargo doc --no-deps --document-private-items
      - run: cargo doc --no-deps --document-private-items --all-features
      - name: Check CHANGELOG has an entry for the current version
        shell: bash
        run: |
          version=$(sed -n 's/^version = "\(.*\)"/\1/p' Cargo.toml | head -1)
          if ! grep -q "^## \[$version\]" CHANGELOG.md; then
            echo "Cargo.toml is at $version but CHANGELOG.md has no '## [$version]' entry" >&2
            exit 1
          fi

  coverage:
    name: Coverage (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-rust
        with:
          components: llvm-tools-preview
      - uses: taiki-e/install-action@cargo-llvm-cov
      - run: cargo llvm-cov --all-features --workspace --lcov --output-path lcov.info
      - uses: codecov/codecov-action@v4
        with:
          files: lcov.info
          token: ${{ secrets.CODECOV_TOKEN }}
